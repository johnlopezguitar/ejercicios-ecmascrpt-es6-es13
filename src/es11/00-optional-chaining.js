const users = {
  gndx: {
    country: "MX",
  },
  ana: {
    country: "CO",
  },
};

console.log(users.gndx.country);

// optional caining para preguntar si exite una llave en el objeto

console.log(users?.bebeloper?.country);
