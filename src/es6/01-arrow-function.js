/* arrow function */

function square(num) {
  return num * num;
}

/*arrow function*/
const square = (num) => {
  return num * num;
};
// arrow funtion con el return implicito para una funcion de una solo linea
const square = (num) => num * num;
